services:
  social-media-planner:
    build: .
    container_name: social-media-planner
    ports:
      - "3001:3000"
    environment:
      PORT: 3000
      DB_PATH: /app/data/planner.db
      ADMIN_USERNAME: admin
      ADMIN_PASSWORD: bitte-sofort-aendern-123
      SESSION_DAYS: 14
    volumes:
      - ./data:/app/data
    restart: unless-stopped
