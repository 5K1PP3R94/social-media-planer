services:
  nora:
    build: .
    container_name: nora-social-planner
    ports:
      - "3001:3001"
    environment:
      PORT: 3001
      DB_PATH: /app/data/planner.db
      SECRET_KEY: change-this-in-portainer
    volumes:
      - ./data:/app/data
    restart: unless-stopped
